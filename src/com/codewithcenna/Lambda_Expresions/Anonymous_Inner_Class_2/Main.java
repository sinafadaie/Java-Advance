package com.codewithcenna.Lambda_Expresions.Anonymous_Inner_Class_2;

//sometimes we don't want to explicitly create a class to implement an interface
//like pre step, we will not reuse ConsolePrinter class
//because this requires writing a bit of code,
//we use Anonymous Inner Class for this(implement interface once for a single purpose)
//it is anonymous because it hasn't named
//it is inner because, its define inside a method
public class Main {
    public static void main(){
        LambdasDemo.show();
    }
}
