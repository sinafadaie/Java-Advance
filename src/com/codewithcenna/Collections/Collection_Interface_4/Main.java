package com.codewithcenna.Collections.Collection_Interface_4;

//collections interface, represent collection objects that acts as a
//collection or a container for other objects
//we should to able add(),remove(),contains(),.. to a collection
//it's a generic object (E is convention(Element))
//collection interface is extended to Iterable interface(all collections are iterable)
//so many class implement collection interface, but the most use full one is **ArrayList**

public class Main {
    public static void main(){
        CollectionDemo.show();
    }
}
